[pytest]
# Lets `import app...` and `import scripts...` resolve when pytest is run
# from backend/ without needing an editable install - same assumption the
# scripts/ entrypoints make (see README: "Run from the backend/ directory").
pythonpath = .
testpaths = tests
