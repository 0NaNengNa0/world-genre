# Extractors, cleansing, and the Postgres load step - i.e. everything the
# Airflow tasks need. This is the ONLY requirements file installed into the
# Airflow image (see Dockerfile); the API's deps deliberately stay out of it,
# because Airflow 3 ships its own FastAPI/pydantic and pinning different
# versions alongside them breaks the Airflow api-server.
#
# For local development you usually want requirements.txt instead, which
# pulls this in plus the API deps.
requests>=2.31.0
beautifulsoup4>=4.12.0
# Never imported directly - bs4 loads it by name from BeautifulSoup(..., "lxml").
# Removing it breaks the kworb scraper at runtime, not at import.
lxml>=5.0.0
python-dotenv>=1.0.0
rapidfuzz>=3.9.0
