# Local dev + CI only. Never installed into the Airflow containers.
#
# Mostly not needed at runtime - but sqlglot IS, for scripts/run_check_schema.py,
# which parses the declared DDL to compare it against INFORMATION_SCHEMA. The
# deploy workflow's validate-sql job therefore installs this file, and a CI run
# failed with exit 2 on 2026-09-16 when it did not.
pytest>=8.0.0
ruff>=0.6.0
# ISO 3166-1 country names for scripts/generate_countries_seed.py. Dev-only
# because it regenerates a committed seed file - nothing at runtime imports it.
pycountry>=24.6.1
# Required by fastapi.testclient (starlette imports it lazily and raises at
# import time if it's missing), used to exercise the API through the real ASGI
# stack in tests/test_published.py.
#
# This said `httpx2` until 2026-09-16 - a different PyPI package entirely. The
# suite passed anyway because that package happens to depend on httpx, so the
# import this actually needs arrived transitively. A dependency that works by
# accident is one upstream release away from not working, and the failure would
# have been an ImportError in CI with no obvious cause.
httpx>=0.27.0
# Parses the BigQuery SQL offline so dialect errors fail in CI rather than
# in a Cloud Run job. There is no local BigQuery emulator, so this is the
# only way to check the .sql files without credentials and a network.
#
# Also a hard dependency of scripts/run_check_schema.py, which is an
# operational script rather than a test - see the note at the top of this file.
# It fails with an actionable message naming this file rather than an
# ImportError, which is how the missing install was diagnosed in one line.
sqlglot>=25.0.0
